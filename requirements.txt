# Install with: pip install -r requirements.txt

# Core
openai>=1.30.0
# OpenAI SDK <2.x passes `proxies=` to httpx.Client; httpx 0.28 removed it — pin until you upgrade openai.
httpx>=0.27.0,<0.28
tiktoken>=0.7.0
python-dotenv>=1.0.0

# Scraping (notebook 01)
httpx>=0.27.0
pdfplumber>=0.11.0
trafilatura
lxml_html_clean>=0.4.0   # PyPI package is 0.x only (no 2.x release)
beautifulsoup4>=4.13.0

# Vector DB
qdrant-client[fastembed]>=1.9.0
fastembed>=0.3.0
onnxruntime==1.19.2

# LangChain
langchain>=0.2.0
langchain-classic>=1.0.0   # MultiQueryRetriever / compression (LC 1.x moved these out of langchain.retrievers)
langchain_text_splitters>=0.2.0
langchain-openai>=0.1.0
langchain-qdrant>=0.1.0
langchain-experimental>=0.0.60   # SemanticChunker lives here

# Reranking
sentence-transformers>=3.0.0
torch==2.5.1+cpu
typing_extensions>=4.12.0

# Evaluation
ragas>=0.1.9
datasets>=2.19.0

# Utilities
pandas>=2.0.0
numpy>=1.26.0
tqdm>=4.66.0

# Notebooks (local Jupyter)
notebook>=7.0.0
ipykernel>=6.0.0
ipywidgets>=8.0.0
