# Generated files — do NOT commit these to git.
# sources.json IS committed (it's the URL manifest, not scraped text).
scraped_corpus.json
clean_chunks.json
chunks/
embeddings/
downloads/
*.json.gz
gt_pairs.json